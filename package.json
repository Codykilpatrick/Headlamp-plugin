{
  "name": "sailor-view",
  "version": "0.1.0",
  "description": "Sailor View — a plain-language Kubernetes dashboard for non-technical operators",
  "main": "dist/main.js",
  "scripts": {
    "build": "headlamp-plugin build && cp package.json dist/package.json",
    "start": "headlamp-plugin start",
    "dev": "./scripts/dev.sh",
    "tsc": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@kinvolk/headlamp-plugin": "^0.13.1",
    "typescript": "^4.9.5"
  },
  "keywords": ["headlamp-plugin"]
}
